# Production env for `npm run build`
# This file is read by Vite only when building for production.
# Frontend deployed to GitHub Pages uses these values.

# Poller backend (DigitalOcean — Caddy + Let's Encrypt)
VITE_POLLER_WS=wss://api.chemafmp.dev

# Supabase (same as dev — already public anon key)
VITE_SUPABASE_URL=https://jryorwbomnilewfrdmrg.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_S_EhAvWaJRkZBPk1YrslxQ_DBwmAZuZ

# LAB views (simulated fleet) — set to "false" to hide in production
VITE_SHOW_LAB=false
